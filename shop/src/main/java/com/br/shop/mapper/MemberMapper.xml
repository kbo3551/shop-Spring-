<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.br.shop.mapper.MemberMapper">
<insert id="insertMember" parameterType="com.br.shop.vo.Member">
	INSERT INTO
			member(
				member_id,
				member_pw,
				member_level,
				member_age,
				member_gender,
				update_date,
				create_date
				)
		VALUES(
			#{memberId},
			PASSWORD(#{memberPw}),
			'0',
			#{memberAge},
			#{memberGender},
			NOW(),
			NOW()
			);
</insert>

</mapper>